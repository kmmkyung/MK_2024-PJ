import { DefaultTheme } from "styled-components";

export const theme:DefaultTheme = {
  color:{
    bgColor: '#1A1D29',
    pointColor: '#32c2ff',
  },
  fontSize:{
    xl: 'clamp(2.4rem, 3vw, 4rem)',
    l: 'clamp(2rem, 2vw, 3rem)',
    m: 'clamp(1.2em, 1.8vw, 1.6rem)',
    s: 'clamp(1em, 1.6vw, 1.2rem)'
  }
}
'use client'

export default function ReviewForm(){
  // const [ state, action ] = useActionState(saveReview, null)

  return (
    <>daf</>
    //  <form action={action} className="w-full">
    //     <div className="my-10">
    //       <textarea name="review" placeholder="거래 후기를 5자 이상 작성해주세요" className="align-middle h-20 text-sm bg-transparent rounded-md w-full ring-2 focus:ring-3 ring-neutral-400 focus:ring-primary border-none placeholder:text-neutral-400 transition-all" />
    //       {state?.formErrors && <p className="text-red-500 mt-3 text-sm">{state.formErrors}</p>}
    //     </div>
    //     <Button text="리뷰 제출하기"/>
    //   </form> 
    
  )
}